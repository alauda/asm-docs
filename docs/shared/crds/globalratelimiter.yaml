apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: globalratelimiters.asm.alauda.io
spec:
  group: asm.alauda.io
  names:
    kind: GlobalRateLimiter
    listKind: GlobalRateLimiterList
    plural: globalratelimiters
    shortNames:
    - grl
    singular: globalratelimiter
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GlobalRateLimiter is the Schema for the globalratelimiters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GlobalRateLimiterSpec defines the desired state of GlobalRateLimiter
            properties:
              domain:
                description: The domain to limit (globally unique).
                type: string
              failureModeDeny:
                description: Failure to deny access mode.
                type: boolean
              rules:
                description: List of rate limit rule.
                items:
                  properties:
                    descriptors:
                      description: List of rate limit description information.
                      items:
                        properties:
                          kind:
                            description: Kind of rate limits.
                            type: string
                          match:
                            description: Rate limit matching.
                            properties:
                              key:
                                description: Match item (required for request header
                                  matching).
                                type: string
                              method:
                                description: Match method.
                                enum:
                                - eq
                                - ne
                                - regex
                                type: string
                              value:
                                description: Match value.
                                type: string
                            required:
                            - method
                            type: object
                        required:
                        - kind
                        - match
                        type: object
                      type: array
                    name:
                      description: Name.
                      type: string
                    rateLimit:
                      description: Current limit value configuration.
                      properties:
                        fixedWindow:
                          description: Fixed window.
                          properties:
                            requestsPerUnit:
                              description: Number of requests per time unit.
                              format: int64
                              type: integer
                            unit:
                              description: Time unit.
                              enum:
                              - second
                              - minute
                              - hour
                              - day
                              type: string
                          required:
                          - requestsPerUnit
                          - unit
                          type: object
                      type: object
                    shadowMode:
                      description: Shadow mode.
                      type: boolean
                    updatedUtc:
                      description: Timestamp of update time(s).
                      format: int64
                      type: integer
                  required:
                  - descriptors
                  - name
                  - rateLimit
                  type: object
                type: array
              serviceType:
                description: Service type.
                enum:
                - micro-service
                - ingress-gateway
                type: string
            required:
            - domain
            - failureModeDeny
            - rules
            - serviceType
            type: object
          status:
            description: GlobalRateLimiterStatus defines the observed state of GlobalRateLimiter
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
