
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: gatewayprojectbindings.asm.alauda.io
spec:
  group: asm.alauda.io
  names:
    kind: GatewayProjectBinding
    listKind: GatewayProjectBindingList
    plural: gatewayprojectbindings
    shortNames:
    - gpb
    singular: gatewayprojectbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gatewayRef
      name: Gateway
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatewayProjectBinding is the Schema for the gatewayprojectbindings
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GatewayProjectBindingSpec defines the desired state of GatewayProjectBinding
            properties:
              gatewayRef:
                description: Foo is an example field of GatewayProjectBinding. Edit
                  gatewayprojectbinding_types.go to remove/update
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - gatewayRef
            type: object
          status:
            description: GatewayProjectBindingStatus defines the observed state of
              GatewayProjectBinding
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
