apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: gatewaygroups.asm.alauda.io
spec:
  group: asm.alauda.io
  names:
    kind: GatewayGroup
    listKind: GatewayGroupList
    plural: gatewaygroups
    singular: gatewaygroup
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatewayGroup is the Schema for the gatewaygroups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GatewayGroupSpec defines the desired state of GatewayGroup
            properties:
              apiPrefixPath:
                description: Access path to the API group.
                type: string
              apiPrefixRule:
                description: Access rules for the API group.
                type: string
              apiRoute:
                description: API group.
                items:
                  properties:
                    method:
                      description: Http Request method.
                      type: string
                    msname:
                      description: Service name.
                      type: string
                    namespace:
                      description: namespace.
                      type: string
                    operationid:
                      description: operation id。
                      type: string
                    path:
                      description: Request path.
                      type: string
                    port:
                      description: Service port.
                      type: integer
                    status:
                      description: Status of group.
                      type: string
                  required:
                  - method
                  - msname
                  - namespace
                  - operationid
                  - path
                  - port
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              gateway:
                type: string
              host:
                description: Access method (domain or non-domain).
                type: string
              name:
                description: Group name.
                type: string
              publish:
                description: Whether to publish.
                type: boolean
            required:
            - name
            - publish
            type: object
          status:
            description: GatewayGroupStatus defines the observed state of GatewayGroup
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
