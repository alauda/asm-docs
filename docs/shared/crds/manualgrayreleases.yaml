apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: manualgrayreleases.asm.alauda.io
spec:
  group: asm.alauda.io
  names:
    kind: ManualGrayRelease
    listKind: ManualGrayReleaseList
    plural: manualgrayreleases
    singular: manualgrayrelease
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.desiredStep
      name: DesiredStep
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManualGrayRelease is the Schema for the ManualGrayRelease API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ManualGrayReleaseSpec defines the desired state of ManualGrayRelease
            properties:
              deliverType:
                description: deliver type, canary or ab or others
                type: string
              desiredStep:
                description: whether the canary deliver is form table or yaml creat
                type: string
              from:
                description: whether the canary deliver is form table or yaml creat
                type: string
              grayWorkloads:
                items:
                  properties:
                    replicas:
                      description: Name of the referent
                      type: integer
                    suffix:
                      description: canary resources suffix
                      type: string
                    templateSpec:
                      description: the workload template spec
                      type: string
                  required:
                  - suffix
                  - templateSpec
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              rollBack:
                description: check for if should cancle gray release
                type: boolean
              targetRef:
                description: TargetRef references a target resource
                properties:
                  apiVersion:
                    description: API version of the referent
                    type: string
                  kind:
                    description: Kind of the referent
                    type: string
                  name:
                    description: Name of the referent
                    type: string
                  namespace:
                    description: Namespace of the referent
                    type: string
                required:
                - name
                type: object
            required:
            - deliverType
            - desiredStep
            - from
            - grayWorkloads
            - rollBack
            - targetRef
            type: object
          status:
            description: ManualGrayReleaseStatus defines the observed state of ManualGrayRelease
            properties:
              conditions:
                items:
                  description: ManualCanaryCondition is a status condition for a Canary
                  properties:
                    err:
                      description: err if the target not valid
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime of this condition
                      format: date-time
                      type: string
                    message:
                      description: Message associated with this condition
                      type: string
                    phase:
                      description: phase of this condition
                      type: string
                    startTime:
                      description: StartTime of this condition
                      format: date-time
                      type: string
                    step:
                      description: Type of this condition
                      type: string
                  required:
                  - step
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              grayWorkloadDeployTime:
                description: GrayWorkloadDeployTime of this condition
                format: date-time
                type: string
              grayWorkloadReadyTime:
                description: GrayWorkloadReadyTime of this condition
                format: date-time
                type: string
              phase:
                description: ManualGrayPhase is a label for the condition of a canary
                  at the current time
                type: string
              targetRefApplyTime:
                description: TargetRefApplyTime of this condition
                format: date-time
                type: string
              targetRefReadyTime:
                description: TargetRefReadyTime of this condition
                format: date-time
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
