---
weight: 15
i18n:
  title:
    en: Installation
sourceSHA: 4c37de0406e1e4c56b79fc530a7dc774626335f337ec3de95e5c561dd0f2f166
---

:::tip

如果该模块需要单独进行部署，则必须提供部署文档；否则，可能没有必要。

部署文档应包括部署流程、部署配置、卸载流程等。如果部署文档内容较多，可以将其改为目录，并在目录中拆分为多个文档进行介绍。

你可以参考 [部署](https://product-doc-guide.alauda.cn/template/installation/example.html) 查看相应文档的示例。

:::

# 部署

## 安装 XXX

介绍安装组件所需的前提条件。

### 使用 Web 控制台安装 XXX

描述使用 Web 控制台安装组件的过程。

### 使用 CLI 安装 XXX

描述使用 CLI 安装组件的过程。

## 卸载 XXX

介绍卸载组件的流程步骤。通常，需要先删除组件的实例，然后删除组件的 Operator，最后删除组件的 CRD。

## 自定义配置

介绍在安装组件过程中可以自定义的配置选项。
