---
weight: 80
---

:::tip

Display the permissions involved in this functional module using a table format.

You can view a [Permissions Explanation example](https://product-doc-guide.alauda.cn/template/permissions/example.html) for reference.

:::

# Permissions

Remove the code block and replace the `function` parameter value with the `FunctionResource` resource name you want to display:
Use the `<K8sPermissionTable />` component to automatically generate a permissions table. For detailed usage, refer to the [Guide to Automatically Generating Permissions Tables](https://product-doc-guide.alauda.cn/guide/auto_gen_permissions.html).

```mdx
<K8sPermissionTable functions={['<functionName1>', '<functionName2>']} />
```
